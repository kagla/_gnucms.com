{% extends "admin/layout.html.twig" %}
{% block title %}회원 관리 · {{ site.site_name }}{% endblock %}
{% block admin_section %}members{% endblock %}
{% block body %}
<nav class="lumen-admin-crumb" aria-label="현재 위치">
  <a href="{{ url_for('admin.index') }}">관리</a><span>/</span><span>회원</span>
</nav>
<header class="lumen-admin-head">
  <div>
    <h1>회원 관리</h1>
    <p class="lumen-lead">회원 정보를 수정하거나 필요한 경우 이용을 차단합니다. 소유자 권한은 변경되지 않습니다.</p>
  </div>
</header>
{% if saved %}<p class="lumen-notice lumen-notice-success">회원 정보를 저장했습니다.</p>{% endif %}
<form class="lumen-search lumen-admin-search" method="get" action="{{ url_for('admin.members') }}" role="search">
  <div class="lumen-search-field"><span class="lumen-search-icon" aria-hidden="true"></span><input type="search" name="q" value="{{ query }}" placeholder="이메일 또는 표시 이름 검색" aria-label="회원 검색"></div>
  <button class="lumen-btn lumen-btn-primary" type="submit">검색</button>
</form>
<section class="lumen-panel">
  <div class="lumen-table-wrap">
    <table class="lumen-table">
      <thead><tr><th>회원</th><th>가입일</th><th>상태</th><th>관리</th></tr></thead>
      <tbody>
        {% for member in members %}
          <tr>
            <td>
              <strong>{{ member.display_name }}</strong>
              {% if member.is_admin %}<span class="lumen-tag">소유자</span>{% endif %}
              <div class="lumen-table-sub">{{ member.email }}</div>
            </td>
            <td class="lumen-table-meta">{{ member.created_at|date('Y.m.d') }}</td>
            <td><span class="lumen-status-pill {{ member.status == 'active' ? 'is-active' : 'is-blocked' }}">{{ member.status == 'active' ? '활성' : '차단' }}</span></td>
            <td>
              <div class="lumen-row-actions">
                <a class="lumen-btn lumen-btn-sm lumen-btn-ghost" href="{{ url_for('admin.members.edit', {id: member.id}) }}">수정</a>
                <form method="post" action="{{ url_for('admin.members.status', {id: member.id}) }}">
                  <input type="hidden" name="csrf_token" value="{{ csrf_token }}">
                  <button class="lumen-btn lumen-btn-sm lumen-btn-ghost" type="submit">{{ member.status == 'active' ? '차단' : '해제' }}</button>
                </form>
              </div>
            </td>
          </tr>
        {% else %}
          <tr><td colspan="4" class="lumen-table-empty">조건에 맞는 회원이 없습니다.</td></tr>
        {% endfor %}
      </tbody>
    </table>
  </div>
</section>
{% endblock %}
