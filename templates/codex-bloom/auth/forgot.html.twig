{# codex-bloom view: auth/forgot.html.twig #}
{# codex-bloom view: auth/forgot.html.twig #}
{% extends "layout.html.twig" %}
{% import '_icons.html.twig' as ic %}
{% block title %}비밀번호 찾기 · {{ site.site_name }}{% endblock %}
{% block body %}
<div class="auth-shell">
  <section class="codex-bloom-auth-card">
    <header class="codex-bloom-panel-head codex-bloom-panel-head-compact">
      <p class="codex-bloom-section-kicker">{{ ic.mail() }} Password reset</p>
      <h1>비밀번호 찾기</h1>
      <p class="codex-bloom-lead">가입한 이메일로 재설정 링크를 보내드립니다.</p>
    </header>
    <form class="codex-bloom-form codex-bloom-auth-form" method="post" action="{{ url_for('auth.forgot') }}">
      <input type="hidden" name="csrf_token" value="{{ csrf_token }}">
      <label class="codex-bloom-field">{{ ic.mail() }} 이메일<input type="email" name="email" autocomplete="email" required></label>
      <button class="codex-bloom-btn codex-bloom-btn-primary codex-bloom-btn-block" type="submit">{{ ic.mail() }} 재설정 링크 받기</button>
    </form>
    <p class="auth-switch"><a href="{{ url_for('auth.login') }}">{{ ic.user() }} 로그인으로 돌아가기</a></p>
  </section>
</div>
{% endblock %}
