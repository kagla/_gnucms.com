{# codex-bloom view: admin/pages.html.twig #}
{# codex-bloom view: admin/pages.html.twig #}
{% extends "admin/layout.html.twig" %}
{% import '_icons.html.twig' as ic %}
{% block title %}내용 관리 · {{ site.site_name }}{% endblock %}
{% block admin_section %}content{% endblock %}
{% block body %}
<nav class="codex-bloom-admin-crumb" aria-label="현재 위치">
  <a href="{{ url_for('admin.index') }}">관리</a><span>/</span><span>내용</span>
</nav>
<header class="codex-bloom-admin-head">
  <div>
    <h1>내용 관리</h1>
    <p class="codex-bloom-lead">소개·이용안내 같은 사이트 내용을 작성합니다. 공개와 메뉴 표시만 고르면 됩니다.</p>
  </div>
  <div class="codex-bloom-admin-actions">
    <a class="codex-bloom-btn codex-bloom-btn-ghost" href="{{ url_for('admin.content.trash') }}">휴지통{% if trash_count > 0 %} {{ trash_count }}{% endif %}</a>
    <a class="codex-bloom-btn codex-bloom-btn-primary" href="{{ url_for('admin.content.create') }}">{{ ic.plus() }} 내용 만들기</a>
  </div>
</header>
{% if query.saved|default('') == '1' %}<p class="codex-bloom-notice codex-bloom-notice-success">내용을 저장했습니다.</p>{% endif %}
{% if query.deleted|default('') == '1' %}<p class="codex-bloom-notice codex-bloom-notice-success">내용을 휴지통으로 옮겼습니다.</p>{% endif %}
<section class="codex-bloom-panel">
  <div class="codex-bloom-table-wrap">
    <table class="codex-bloom-table">
      <thead><tr><th>제목</th><th>주소</th><th>상태</th><th>메뉴</th><th>관리</th></tr></thead>
      <tbody>
        {% for page in pages %}
          <tr>
            <td>
              {% if page.status == 'published' %}
                <a class="codex-bloom-table-link" href="{{ url_for('content.show', {slug: page.slug}) }}">{{ page.title }}</a>
              {% else %}
                <strong>{{ page.title }}</strong>
              {% endif %}
            </td>
            <td class="codex-bloom-table-meta">/content/{{ page.slug }}</td>
            <td><span class="codex-bloom-status-pill {{ page.status == 'published' ? 'is-active' : 'is-blocked' }}">{{ page.status == 'published' ? '공개' : '초안' }}</span></td>
            <td>{{ page.show_in_menu ? '표시' : '숨김' }}</td>
            <td>
              <div class="codex-bloom-row-actions">
                <a class="codex-bloom-btn codex-bloom-btn-sm codex-bloom-btn-ghost" href="{{ url_for('admin.content.preview', {id: page.id}) }}" target="_blank" rel="noopener">미리보기</a>
                <a class="codex-bloom-btn codex-bloom-btn-sm codex-bloom-btn-ghost" href="{{ url_for('admin.content.edit', {id: page.id}) }}">수정</a>
              </div>
            </td>
          </tr>
        {% else %}
          <tr><td colspan="5" class="codex-bloom-table-empty">아직 등록된 내용이 없습니다.</td></tr>
        {% endfor %}
      </tbody>
    </table>
  </div>
</section>
{% endblock %}
