{# codex-bloom view: admin/_sidebar.html.twig #}
{# codex-bloom view: admin/_sidebar.html.twig #}
{% import '_icons.html.twig' as ic %}
<aside class="admin-sidebar codex-bloom-admin-sidebar" id="admin-sidebar">
  <div class="codex-bloom-admin-sidebar-top">
    <a class="codex-bloom-admin-brand" href="{{ url_for('admin.index') }}">
      <span class="codex-bloom-admin-brand-mark">{{ ic.logo(28) }}</span>
      <span><strong>관리 콘솔</strong><small>{{ site.site_name }}</small></span>
    </a>
    <button class="admin-sidebar-toggle codex-bloom-admin-toggle" type="button" aria-controls="admin-sidebar" aria-label="관리 메뉴 접기" aria-expanded="true" title="관리 메뉴 접기"><span aria-hidden="true">{{ ic.arrow() }}</span></button>
  </div>
  <nav class="admin-nav codex-bloom-admin-nav" aria-label="관리 메뉴">
    <div class="admin-nav-group">
      <span class="admin-nav-label codex-bloom-admin-nav-label">운영</span>
      <a class="admin-nav-item admin-nav-dashboard codex-bloom-admin-nav-item" href="{{ url_for('admin.index') }}" title="대시보드"><span class="admin-nav-icon">{{ ic.home() }}</span><span>대시보드</span></a>
      <a class="admin-nav-item admin-nav-boards codex-bloom-admin-nav-item" href="{{ url_for('admin.boards') }}" title="게시판 관리"><span class="admin-nav-icon">{{ ic.board() }}</span><span>게시판</span></a>
      <a class="admin-nav-item admin-nav-content codex-bloom-admin-nav-item" href="{{ url_for('admin.content') }}" title="내용 관리"><span class="admin-nav-icon">{{ ic.doc() }}</span><span>내용</span></a>
      <a class="admin-nav-item admin-nav-members codex-bloom-admin-nav-item" href="{{ url_for('admin.members') }}" title="회원 관리"><span class="admin-nav-icon">{{ ic.users() }}</span><span>회원</span></a>
    </div>
    <div class="admin-nav-group">
      <span class="admin-nav-label codex-bloom-admin-nav-label">설정</span>
      <a class="admin-nav-item admin-nav-site codex-bloom-admin-nav-item" href="{{ url_for('admin.settings') }}" title="사이트 설정"><span class="admin-nav-icon">{{ ic.settings() }}</span><span>사이트</span></a>
      <a class="admin-nav-item admin-nav-mail codex-bloom-admin-nav-item" href="{{ url_for('admin.mail') }}" title="메일 설정"><span class="admin-nav-icon">{{ ic.mail() }}</span><span>메일</span></a>
      <a class="admin-nav-item admin-nav-legal codex-bloom-admin-nav-item" href="{{ url_for('admin.terms') }}" title="약관 관리"><span class="admin-nav-icon">{{ ic.shield() }}</span><span>약관</span></a>
      <a class="admin-nav-item admin-nav-security codex-bloom-admin-nav-item" href="{{ url_for('admin.password') }}" title="계정 보안"><span class="admin-nav-icon">{{ ic.shield() }}</span><span>보안</span></a>
    </div>
  </nav>
  <a class="admin-site-link codex-bloom-admin-site-link" href="{{ url_for('boards.index') }}" title="사이트 보기"><span>{{ ic.external() }} 사이트 보기</span></a>
  <div class="admin-account codex-bloom-admin-account">
    <span class="admin-account-name">{{ ic.user() }} {{ current_user.display_name }}</span>
    <button class="theme-toggle codex-bloom-theme-toggle codex-bloom-icon-btn" type="button" aria-label="다크 모드로 전환" title="다크 모드로 전환">
      <span class="codex-bloom-theme-moon">{{ ic.moon() }}</span>
      <span class="codex-bloom-theme-sun" hidden>{{ ic.sun() }}</span>
    </button>
    <form class="logout-form" method="post" action="{{ url_for('auth.logout') }}">
      <input type="hidden" name="csrf_token" value="{{ csrf_token }}">
      <button class="logout-button codex-bloom-btn codex-bloom-btn-ghost" type="submit" title="로그아웃"><span class="admin-logout-icon">{{ ic.logout() }}</span><span class="admin-logout-label">로그아웃</span></button>
    </form>
  </div>
</aside>
