{% if oauth_providers is not empty %}
  <div class="hz-auth-divider"><span>또는</span></div>
  <div class="hz-social-login social-login" aria-label="소셜 로그인">
    {% for provider in oauth_providers %}
      <a class="hz-social-btn social-button social-{{ provider.key }}" href="{{ url_for('oauth.start', {provider: provider.key}) }}">
        <span class="hz-social-icon social-symbol" aria-hidden="true">
          {% if provider.key == 'google' %}G{% elseif provider.key == 'naver' %}N{% elseif provider.key == 'kakao' %}K{% else %}⌘{% endif %}
        </span>
        {{ provider.label }}로 계속하기
      </a>
    {% endfor %}
  </div>
  {% include "auth/_social_consent.html.twig" %}
{% endif %}
