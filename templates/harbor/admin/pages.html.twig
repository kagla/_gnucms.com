{# harbor view: admin/pages.html.twig #}
{% extends "admin/layout.html.twig" %}
{% import '_icons.html.twig' as ic %}
{% block title %}내용 관리 · {{ site.site_name }}{% endblock %}
{% block admin_section %}content{% endblock %}
{% block body %}
<nav class="harbor-admin-crumb" aria-label="현재 위치">
  <a href="{{ url_for('admin.index') }}">관리</a><span>/</span><span>내용</span>
</nav>
<header class="harbor-admin-head">
  <div>
    <h1>내용 관리</h1>
    <p class="harbor-lead">소개·이용안내 같은 사이트 내용을 작성합니다. 공개와 메뉴 표시만 고르면 됩니다.</p>
  </div>
  <div class="harbor-admin-actions">
    <a class="harbor-btn harbor-btn-ghost" href="{{ url_for('admin.content.trash') }}">휴지통{% if trash_count > 0 %} {{ trash_count }}{% endif %}</a>
    <a class="harbor-btn harbor-btn-primary" href="{{ url_for('admin.content.create') }}">{{ ic.plus() }} 내용 만들기</a>
  </div>
</header>
{% if query.saved|default('') == '1' %}<p class="harbor-notice harbor-notice-success">내용을 저장했습니다.</p>{% endif %}
{% if query.deleted|default('') == '1' %}<p class="harbor-notice harbor-notice-success">내용을 휴지통으로 옮겼습니다.</p>{% endif %}
<section class="harbor-panel">
  <div class="harbor-table-wrap">
    <table class="harbor-table">
      <thead><tr><th>제목</th><th>주소</th><th>상태</th><th>메뉴</th><th>관리</th></tr></thead>
      <tbody>
        {% for page in pages %}
          <tr>
            <td>
              {% if page.status == 'published' %}
                <a class="harbor-table-link" href="{{ url_for('content.show', {slug: page.slug}) }}">{{ page.title }}</a>
              {% else %}
                <strong>{{ page.title }}</strong>
              {% endif %}
            </td>
            <td class="harbor-table-meta">/content/{{ page.slug }}</td>
            <td><span class="harbor-status-pill {{ page.status == 'published' ? 'is-active' : 'is-blocked' }}">{{ page.status == 'published' ? '공개' : '초안' }}</span></td>
            <td>{{ page.show_in_menu ? '표시' : '숨김' }}</td>
            <td>
              <div class="harbor-row-actions">
                <a class="harbor-btn harbor-btn-sm harbor-btn-ghost" href="{{ url_for('admin.content.preview', {id: page.id}) }}" target="_blank" rel="noopener">미리보기</a>
                <a class="harbor-btn harbor-btn-sm harbor-btn-ghost" href="{{ url_for('admin.content.edit', {id: page.id}) }}">수정</a>
              </div>
            </td>
          </tr>
        {% else %}
          <tr><td colspan="5" class="harbor-table-empty">아직 등록된 내용이 없습니다.</td></tr>
        {% endfor %}
      </tbody>
    </table>
  </div>
</section>
{% endblock %}
