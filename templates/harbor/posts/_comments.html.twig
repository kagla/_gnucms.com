{# harbor view: posts/_comments.html.twig #}
{% import '_icons.html.twig' as ic %}
<ul class="harbor-comment-list{% if harbored|default(false) %} harbor-comment-children{% endif %}">
{% for comment in nodes %}
  <li class="harbor-comment">
    <div class="harbor-comment-head">
      {% if not comment.deleted %}<span class="harbor-comment-author">{{ ic.user() }} {{ comment.author_name }}</span>{% else %}<span class="muted">{{ ic.comment() }} 삭제된 댓글</span>{% endif %}
      <time datetime="{{ comment.created_at }}">{{ ic.calendar() }} {{ comment.created_at }}</time>
      {% if comment.is_secret %}<span class="harbor-tag harbor-tag-muted" title="비밀 댓글">{{ ic.shield() }}</span>{% endif %}
    </div>
    <p class="harbor-comment-body">{{ comment.content|nl2br }}</p>
    {% if comment.children is not empty %}
      {% include 'posts/_comments.html.twig' with {'nodes': comment.children, 'harbored': true} only %}
    {% endif %}
  </li>
{% endfor %}
</ul>
