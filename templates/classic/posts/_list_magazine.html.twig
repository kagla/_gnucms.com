{# 매거진형: 썸네일 + 발췌문을 나란히. 읽을거리 중심 게시판에 맞는다. #}
{% include 'posts/_notices.html.twig' %}
{% if list.data is empty %}
  <div class="surface"><p class="empty">조건에 맞는 글이 없습니다.</p></div>
{% else %}
  <ul class="magazine-list surface">
    {% for post in list.data %}
      <li class="magazine-item">
        <a class="magazine-link" href="{{ url_for('posts.show', {'id': post.id}) }}">
          <span class="magazine-thumb">{% include 'posts/_thumb.html.twig' %}</span>
          <span class="magazine-body">
            <span class="magazine-head">
              {% if board.use_category and post.category %}<span class="tag tag-quiet">{{ post.category }}</span>{% endif %}
              {% if post.is_secret %}<span class="post-markers" title="비밀글" aria-label="비밀글">●</span>{% endif %}
              <span class="magazine-title">{{ post.title }}</span>
            </span>
            {% if post.excerpt|default(null) %}<span class="magazine-excerpt">{{ post.excerpt }}</span>{% endif %}
            <span class="magazine-meta">
              <span>{{ post.author_name }}</span>
              <span class="dot" aria-hidden="true">·</span>
              <time datetime="{{ post.created_at }}">{{ post.created_at|date('Y.m.d') }}</time>
              <span class="dot" aria-hidden="true">·</span>
              <span>조회 {{ post.view_count }}</span>
              {% if post.comment_count > 0 %}<span class="comment-count">{{ post.comment_count }}</span>{% endif %}
              {% if post.file_count > 0 %}<span class="post-markers" title="첨부파일 있음" aria-label="첨부파일 있음">＋</span>{% endif %}
            </span>
          </span>
        </a>
      </li>
    {% endfor %}
  </ul>
{% endif %}
