<aside class="admin-sidebar" id="admin-sidebar">
  <div class="admin-sidebar-head">
    <a class="admin-home-link" href="{{ url_for('admin.index') }}"><span class="admin-console-icon" aria-hidden="true">▦</span><strong>관리 콘솔</strong></a>
    <button class="admin-sidebar-toggle" type="button" aria-controls="admin-sidebar" aria-label="관리 메뉴 접기" aria-expanded="true" title="관리 메뉴 접기"><span aria-hidden="true">‹</span></button>
  </div>
  <nav class="admin-nav" aria-label="관리 메뉴">
    <div class="admin-nav-group"><span class="admin-nav-label">운영</span>
      <a class="admin-nav-item admin-nav-dashboard" href="{{ url_for('admin.index') }}" title="대시보드"><span class="admin-nav-icon" aria-hidden="true">⌂</span><span>대시보드</span></a>
      <a class="admin-nav-item admin-nav-boards" href="{{ url_for('admin.boards') }}" title="게시판 관리"><span class="admin-nav-icon" aria-hidden="true">▤</span><span>게시판 관리</span></a>
      <a class="admin-nav-item admin-nav-content" href="{{ url_for('admin.content') }}" title="내용 관리"><span class="admin-nav-icon" aria-hidden="true">□</span><span>내용 관리</span></a>
      <a class="admin-nav-item admin-nav-members" href="{{ url_for('admin.members') }}" title="회원 관리"><span class="admin-nav-icon" aria-hidden="true">♙</span><span>회원 관리</span></a>
    </div>
    <div class="admin-nav-group"><span class="admin-nav-label">설정</span>
      <a class="admin-nav-item admin-nav-site" href="{{ url_for('admin.settings') }}" title="사이트 설정"><span class="admin-nav-icon" aria-hidden="true">◇</span><span>사이트 설정</span></a>
      <a class="admin-nav-item admin-nav-mail" href="{{ url_for('admin.mail') }}" title="메일 설정"><span class="admin-nav-icon" aria-hidden="true">✉</span><span>메일 설정</span></a>
      <a class="admin-nav-item admin-nav-legal" href="{{ url_for('admin.terms') }}" title="약관 관리"><span class="admin-nav-icon" aria-hidden="true">§</span><span>약관 관리</span></a>
      <a class="admin-nav-item admin-nav-security" href="{{ url_for('admin.password') }}" title="계정 보안"><span class="admin-nav-icon" aria-hidden="true">○</span><span>계정 보안</span></a>
    </div>
  </nav>
  <a class="admin-site-link" href="{{ url_for('boards.index') }}" title="사이트 보기"><span>사이트 보기</span><span aria-hidden="true">↗</span></a>
  <div class="admin-account">
    <span class="admin-account-name">{{ current_user.display_name }}</span>
    <button class="theme-toggle" type="button" aria-label="다크 모드로 전환" title="다크 모드로 전환"><span aria-hidden="true">☾</span></button>
    <form class="logout-form" method="post" action="{{ url_for('auth.logout') }}"><input type="hidden" name="csrf_token" value="{{ csrf_token }}"><button class="logout-button" type="submit" title="로그아웃"><span class="admin-logout-icon" aria-hidden="true">↪</span><span class="admin-logout-label">로그아웃</span></button></form>
  </div>
</aside>
